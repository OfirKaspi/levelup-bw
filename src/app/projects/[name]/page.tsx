// import { fetchProjectByName } from '@/lib/api/projects';

// interface ProjectPageProps {
//   params: { name: string };
// }

// export default async function ProjectDetails({ params }: ProjectPageProps) {
//   const project = await fetchProjectByName(params.name);

//   return (
//     <div>
//       <h1>{project.name}</h1>
//       <p>{project.description}</p>
//     </div>
//   );
// }
